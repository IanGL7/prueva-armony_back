services:
  - type: web
    name: armony-backend
    env: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: OPENAI_API_KEY
        sync: false  # añade el secreto en el Dashboard de Render
